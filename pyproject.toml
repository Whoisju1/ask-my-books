[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ask-my-books"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]
